# DNLS V4 deposit figure naming

The V4 figure naming is now intrinsic to the plotting scripts; no manual relabeling step is required between script output and Zenodo deposit filenames.

## Deposit-facing outputs emitted directly

- `fig6_d2_natural.pdf` (also keeps canonical `fig_A_d2_scaling.png`)
- `fig7_fss_T1e4.pdf` (also keeps canonical `fig_B_nstability.png`)
- `fig8_ratio_collapse.pdf` (also keeps canonical `fig_D_homogenization.png`)
- `fig9_T1e6_saturation.pdf` (from `analyze_long_time.py`)

For completeness, the paper-figure script path also emits `fig_C_inversion.pdf` while retaining `fig_C_inversion.png`.
